import type { Suit } from 'engine';
import type { Component } from 'solid-js';

export interface FoundationSlotProps {
  suit: Suit;
}

export const FoundationSlot: Component<FoundationSlotProps> = (_props) => {
  return null;
};
