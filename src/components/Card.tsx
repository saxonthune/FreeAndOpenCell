import type { Component } from 'solid-js';
import type { Card as CardData } from 'engine';

export interface CardProps {
  card: CardData;
}

export const Card: Component<CardProps> = (_props) => {
  return null;
};
